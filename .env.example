# Copy to `.env.local` for local overrides (never commit secrets).
#
# Optional — only if you add analytics or env-based config later:
# NEXT_PUBLIC_SITE_URL=https://rootsconnect.be
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=rootsconnect.be
#
# Google Search Console site verification (meta content value).
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
