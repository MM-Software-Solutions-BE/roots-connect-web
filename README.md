# roots-connect-web

Stack: **Next.js** (App Router) · **Tailwind CSS** · **shadcn/ui**

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Add shadcn components with `npx shadcn@latest add <component>`.
